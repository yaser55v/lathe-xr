
import { AssetManager, Box3, Vector3 } from "@iwsdk/core";

// This is a temporary script to inspect the DOOR object in the loaded GLB
// We can't run this directly as a standalone script easily without the full SDK environment,
// but we can look at the code to plan the implementation.
